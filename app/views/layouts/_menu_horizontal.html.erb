<nav class="navbar navbar-light bg-primary fixed-top">
  <div class="container">
    <div class="row w-100 pt-4 pb-4">

      <div class="col-lg-3 col-md-12 mb-1">
      <%= link_to "H.Campestre 🌴", root_path, class: 'text-decoration-none text-white fs-3'  %>
      </div>

      <div class="col-lg-7 col-md-12">
        <%= form_with(url:"/", method: "get", class:'row') do |formulario| %>
          <div class="col-8 text-center">
            <%= formulario.text_field :busqueda, class:'form-control', placeholder: "Buscar hotel o ciudad" %>
          </div>

          <div class="col-4 text-start">
            <%= formulario.submit "Buscar", class: 'btn btn-secondary text-white' %>
          </div>
        <% end %>
      </div>

    <div class="col-lg-2">
      <% if @usuario %>
        <%= link_to "🙎‍♂️ #{@usuario.nombre}", "#", class: "btn btn-secondary text-white" %>
      <% else %>
        <%= link_to "💻 Registrarse", registro_path, class:"btn btn-secondary text-white" %>
      <% end %>
    </div>

    </div>
  </div>
</nav>